services:
  - type: web
    name: shopify-chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GROQ_API_KEY
        sync: false        # means you'll enter this manually in Render dashboard
      - key: PYTHONUTF8
        value: "1"
